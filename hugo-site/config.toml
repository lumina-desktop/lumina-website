baseURL = "https://lumina-desktop.github.io/"
languageCode = "en-us"
defaultContentLanguage = "en"
title = "Lumina Desktop Environment"
theme = "hugo-theme-learn"
copyright = "(c) 2012-2018 : Ken Moore"

[author]
  name = "Ken Moore"

[outputs]
home = [ "HTML", "RSS", "JSON"]

[privacy]
  [privacy.disqus]
    disable = true
  [privacy.googleAnalytics]
    disable = true
  [privacy.instagram]
    disable = true
  [privacy.twitter]
    disable = true
  [privacy.vimeo]
    disable = true
  [privacy.youtube]
    disable = true

[params]
  # Prefix URL to edit current page. Will display an "Edit this page" button on top right hand corner of every page. 
  # Useful to give opportunity to people to create merge request for your doc.
  # See the config.toml file from this documentation site to have an example.
  editURL = "https://github.com/lumina-desktop/lumina-desktop.github.io/hugo-site/content/"
  # Author of the site, will be used in meta information
  author = "Ken Moore"
  copyrightyear = "2012-2018"
  # Description of the site, will be used in meta information
  description = "Lumina desktop environment project page"
  # Shows a checkmark for visited pages on the menu
  showVisitedLinks = false
  # Disable search function. It will hide search bar
  disableSearch = false
  # Javascript and CSS cache are automatically busted when new version of site is generated. 
  # Set this to true to disable this behavior (some proxies don't handle well this optimization)
  disableAssetsBusting = false
  # Set this to true to disable copy-to-clipboard button for inline code.
  disableInlineCopyToClipBoard = false
  # A title for shortcuts in menu is set by default. Set this to true to disable it. 
  disableShortcutsTitle = true
  # When using mulitlingual website, disable the switch language button.
  disableLanguageSwitchingButton = false
  # Order sections in menu by "weight" or "title". Default to "weight"
  ordersectionsby = "weight"
  # Change default color scheme with a variant one. Can be "red", "blue", "green".
  themeVariant = "mine"
  # Social Media Links
  github = "https://github.com/lumina-desktop"
  #twitch = ""
  #twitter = ""
  telegram = "https://t.me/luminadesktop"
